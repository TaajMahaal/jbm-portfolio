{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@uppy/context7"],
      "description": "Live documentation for React, Astro, Tailwind, Framer Motion, and Three.js"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "GitHub integration for issues, PRs, and repository management (requires gh CLI)"
    }
  }
}
